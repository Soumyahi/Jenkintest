pipeline {

    agent any

    parameters {
        string(name: 'USERNAME', defaultValue: 'Soumya')
    }

    stages {

        stage('Parameter') {
            steps {
                echo "Username is ${params.USERNAME}"
            }
        }

    }

}
